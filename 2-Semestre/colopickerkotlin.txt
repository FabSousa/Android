https://www.geeksforgeeks.org/how-to-create-a-basic-color-picker-tool-in-android/

val colorPickerDialogue = AmbilWarnaDialog(this, 0xffffff,
                object : AmbilWarnaDialog.OnAmbilWarnaListener {
                    override fun onCancel(dialog: AmbilWarnaDialog?) {
                        // leave this function body as
                        // blank, as the dialog
                        // automatically closes when
                        // clicked on cancel button
                    }

                    override fun onOk(dialog: AmbilWarnaDialog?, color: Int) {
                        // change the mDefaultColor to
                        // change the GFG text color as
                        // it is returned when the OK
                        // button is clicked from the
                        // color picker dialog

                        val hexColor = String.format("#%06X", 0xFFFFFF and color)

                        var intent: Intent = Intent(baseContext, ViewActvity::class.java)

                        intent.putExtra("color", hexColor)

                        startActivity(intent)

                        // now change the picked color
                        // preview box to mDefaultColor
                    }
                })
            colorPickerDialogue.show()